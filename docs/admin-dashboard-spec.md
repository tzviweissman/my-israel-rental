# Admin dashboard spec — NOT IN THIS REPO

**This document was never committed.** Three specs cite it by name and a
reader following any of them lands here, so this file exists to say that
plainly rather than to 404. It is a placeholder, not a spec: do not treat
anything below as a ruling.

This is the second time it has happened — `docs/failure-patterns.md`
records four specs cited by name that were never committed and vanished
with the sessions that wrote them. A citation is not a backup.

## Who cites it, and what they say it contained

- `docs/leads-and-views-spec.md:160` — *"A6, where the flow-vs-stock
  distinction was worked out"*. So it had numbered sections, and A6
  settled how a running total (stock) differs from a rate (flow) in the
  admin's numbers.
- `docs/categories-expansion-spec.md:186` — *"a moderation queue in the
  admin (`docs/admin-dashboard-spec.md` already asks for the surfaces)"*.
  So it specified moderation surfaces.
- `docs/perks-and-features-spec.md:102` — *"Perks appear in the admin
  (per `docs/admin-dashboard-spec.md`): list, moderate, deactivate."*

That is the whole of what survives.

## What to do about it

Either write the spec — in which case replace this file entirely — or
edit the three citing lines to stop referring to a document that does not
exist. Leaving it as it was is the one option that keeps costing time:
each of those three lines reads as though the reasoning is written down
somewhere, and it is not.
