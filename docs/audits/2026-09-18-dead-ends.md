# Dead ends — 2026-09-18

Scheduled dead-ends audit, following `.claude/skills/dead-ends/SKILL.md`. Forward
pass (route table + control resolution), backward pass (promise → destination),
and orphaned-capability sweep, cross-referenced against the 2026-09-14 audit
(`docs/audits/2026-09-14-dead-ends.md`) and the fix commit `7dc5b044` from the
(unfiled) 2026-09-17 audit.

**Operational note, not a finding:** this run's assigned worktree branch was
checked out 32 commits behind `origin/main` — it predated the home-page swap,
the pricing-rule unification, the admin demand tracking, account-deletion
cascade, storage-type removal, the Terms page, and the 17 Sep fix commit
entirely (`git merge-base --is-ancestor e2a42fb HEAD` failed; `git log
HEAD..origin/main` listed all 32). None of those commits were in this
worktree's history even though `git log --oneline --since=2026-09-14` (run
from the worktree) showed only the older 22. The working tree was clean with
no unique commits of its own, so I checked out `origin/main` (detached,
read-only, no push) to audit the code that is actually live, and everything
below reflects `origin/main` at `f246070`.

**Headline: nothing new this cycle rises to Broken or Dead-end-promise
severity.** The Terms page 404 (the big one, from the 16 Sep batch) is
already fixed and predates this audit window's "new" findings. One genuinely
new item this cycle: `GradientText`/`gradient-text.jsx` became dead code
today when the hero's white-box fix (`a2ae6b7`) removed its only caller —
cosmetic, no user impact. Everything else new is either a confirmed fix or a
reclassification (subscription/plan endpoints, previously flagged as
orphaned, are now explicitly documented in code comments as intentionally
dormant — "free to list" replaced the paid-plan model). The long carryover
list from 09-14 is essentially unchanged: still open, re-verified, no
regressions.

---

## Broken: goes nowhere or errors

None found.

## Dead-end promises: destination lacks the affordance

None found live. (The one from 09-14 — the stale `tourSteps.js` comment — is
still open; see Orphaned capabilities, since it's stale documentation rather
than a user-facing promise.)

## Wrong destination

**Both carried over from 09-14, unchanged, re-confirmed against current
`Auth.js`:**

- **`frontend/src/pages/Auth.js:116`** (was 113-118 in the 09-14 report; file
  has grown) — in the `mode === 'signup' && formData.role === 'provider'`
  branch, `navigate('/businesses/add')` is still hardcoded instead of
  `navigate(destination)` (used one branch below, line 120, and by every
  other branch). A new comment now sits directly above it explaining the
  *intent* ("Service providers land straight in the gig-creation wizard — no
  property-management upsell") — so this now reads as a considered product
  decision to always send provider signups to the wizard, not an accidental
  bug. It still means an explicit `?redirect=` is silently dropped for
  provider signups specifically. Still low severity: `grep` for
  `/auth/signup` across the frontend is still zero hits; `SignupJoin.jsx`
  (the real entry point) still gets this right. **Fix, if still wanted:**
  either leave as documented-intentional (update the 09-14 finding's
  disposition to "closed as intentional"), or `navigate('/businesses/add')` →
  `navigate(destination)` to restore the redirect for the rare hand-typed
  URL.
- **`frontend/src/pages/Auth.js:621`** — `<a href="/terms" target="_blank">`
  still has no `rel` attribute, while `SignupJoin.jsx:670`'s identical Terms
  link correctly carries `rel="noreferrer"`. Notably, the 16 Sep batch
  (`848a6ba`) touched this exact area to *create* the `/terms` route the
  checkbox links to, and still didn't add the attribute. Same-origin, so
  practical risk is low. **Fix:** add `rel="noreferrer"` to match.

---

## Orphaned capabilities

**NEW since 09-14/09-17:**

- **`frontend/src/components/ui/gradient-text.jsx`** (`GradientText`) — now
  imported by nothing. It was the "without limits" hero word's animated-blob
  effect; commit `a2ae6b7` ("Hero: no white box behind 'without limits',
  tighter gap", today) replaced `<GradientText>…</GradientText>` with a plain
  `<span>` in `HomePreview.jsx` and removed the import, but left the
  component file behind. Pure dead code, no user-facing effect — the same
  pattern as `BlocksHero.jsx` below. **Fix:** delete the file (or keep if a
  near-term revival is expected — check with whoever's driving the hero
  iteration before deleting, since this is the third hero rewrite this
  week).
- **Reclassification, not a new bug:** `POST /marketplace/subscription/cancel`,
  `/upgrade`, `/select-plan` and `GET /subscription/plans` — flagged as
  orphaned in the 09-14 carryover list — are still uncalled, but the frontend
  now says why, in three places: `MyGigsTab.jsx:434-436` ("upgrade() and
  cancelPro() lived here... Both endpoints still exist server-side,
  dormant"), `CreateGig.jsx:504-506` ("Publishing used to be followed by
  /subscription/select-plan and a PayPal handoff... Listing is free, so
  publishing now ends at the gig itself"), and `WhyList.jsx:210-212` ("This
  section used to carry the live commitment ladder from /subscription/plans;
  listing is free, so it states that plainly instead"). This reads as a
  deliberate move to a free-listing model, documented at each site, not an
  unfinished feature. `POST /subscription/activate` is still called (from
  `PaymentSuccess.js`, unrelated to the plan ladder) and `plans/status` +
  `plans/bootstrap` are still called from the admin `PayPalPlansPanel.jsx`.
  **Recommendation:** no code fix needed; if these four routes are truly
  retired, delete them from `backend/routes/marketplace/subscription.py` in
  a separate cleanup — currently harmless dead server code.

**Carried over from 09-14, still open, re-verified this run (no regression,
no fix landed):**

- **`frontend/src/components/tour/tourSteps.js:67-70`** — stale comment about
  a "no QR control" gap that was closed a session before the 09-14 audit
  even ran. Unchanged.
- **`WebsiteOrderIn.pay_by`** (`backend/routes/marketplace/orders.py:1830`) —
  still no frontend setter; `OrderPage.jsx` never sends it.
- **Courier "leave an active business"** — `POST
  /marketplace/courier/businesses/{business_id}/leave` (`orders.py:1320`) is
  still only reachable from `decline_invite` on a *pending* invite
  (`DeliveriesTab.jsx:61`); no self-service "stop delivering" control exists
  for an already-accepted business. Confirmed via case-insensitive grep for
  "leave" in `DeliveriesTab.jsx` — zero hits outside the pending-decline
  path.
- **`frontend/src/components/home/BlocksHero.jsx`** — still dead, still
  unimported (confirmed via the orphan-component sweep this run).
- **`contractManager.noContracts` / `contractManager.uploadFirst`**
  (`frontend/src/locales/en.js:2493-2494`, `he.js:2380-2381`) — still
  orphaned locale keys, no component reference.
- **`RequestIn.furnished` / `RequestIn.subcategory`**
  (`backend/routes/marketplace/requests.py`, `furnished` and `subcategory`
  fields) — `PostRequest.jsx` still never sets either.
- **`PropertyCreate.ical_url`** (singular, `backend/models.py:133`) — still
  dead vs. the real `ical_urls` array; only read by `onboarding.py`'s
  completeness check, never written by any form.
- **`checkin_time` / `checkout_time`** — still only reachable via
  `BulkEditModal.jsx`; no per-listing input field exists in
  `AddPropertyModal.jsx`.
- **`GET /marketplace/businesses/{id}/listings`** — no frontend caller
  (confirmed: no axios call anywhere matches this path; `.listings` field
  accesses elsewhere read data already embedded in a business object from a
  different endpoint, not this one).
- **`GET /admin/marketplace/summary`** — no frontend caller.
- **`GET /properties/featured`** — no frontend caller. Worth noting: the
  backend comment introducing this endpoint (`browse.py`, "Home page
  featured strip") explains it was built to replace `Home.js`'s "fetch the
  entire catalog and filter client-side" pattern — but `Home.js` is the page
  the 19 Sep swap just un-routed, so the one page this endpoint was built
  for no longer renders at all. `HomePreview.jsx` (the current `/`) doesn't
  call it either. Marginally more orphaned than before, not less.
- **`GET /marketplace/requests` `category`/`rental_kind` query params** —
  still unsent by the Requests board frontend.
- **`GET /properties/check-duplicate` `area`/`title` fallback** — still
  unreachable; `AddPropertyModal.jsx`'s poll effect still early-returns
  unless `address` is present, so the address-less fallback path the backend
  supports is never exercised from the UI.
- **`GET /admin/gigs` `status` filter** — still no frontend control;
  `ServicesTab.jsx` only ever sends `q` (search text), never `status`.
- **`posts_awaiting_moderation`** (`backend/routes/admin/marketplace.py:274`,
  part of the `admin_attention` response) — computed but not rendered.
  `frontend/src/components/admin/AttentionQueue.jsx` renders every other
  field from that same response (`requests_expiring_unanswered`,
  `chats_unanswered`, `services_without_photo`, `businesses_unverified`,
  `emails_bounced_7d`) but not this one. Explicitly flagged as still-open in
  `7dc5b044`'s own commit message ("Not done from the dead-ends audit: the
  unused 'posts awaiting moderation' count. Harmless, its own cleanup.") —
  confirmed unchanged since. Note this is distinct from the *separate*,
  already-fixed `GET /admin/request-reports` moderation queue
  (`RequestReportsTab.jsx`, wired since `b21a225`) — the underlying
  `requests.report_count`/`needs_review` data now has a real UI via that
  tab; only this redundant summary-count field in the KPI strip is unused.

**Enum values:** nothing newly orphaned. Order/courier/payment status values
still all have matching UI controls.

**Scheduled work:** all 13 `asyncio.create_task` registrations in
`backend/server.py` unchanged from 09-14, all still accounted for. The new
`backend/scripts/live_price_check.py` (from `d37f724`) is correctly *not* one
of these — it's a standalone, read-only script invoked by the site-audit
skill's nightly routine, not an in-process loop, and its own docstring says
so.

## Wrong destination — reclassified as fixed, confirmed by diff

- **`/terms`** — the 09-14/09-15 audits' biggest structural finding (both
  signup screens' required "I agree to Terms & Privacy" checkbox linked to a
  route that never existed) is fixed by `848a6ba`/`4486e3b`. `Terms.jsx`
  exists, is routed at `/terms`, and its own file-header comment documents
  the original bug for posterity. Both `Auth.js:621` and
  `SignupJoin.jsx:670` now resolve to a real page. Locale parity confirmed
  (32 `terms.*` keys in both `en.js` and `he.js`).

---

## Verified working

- **Home page swap (`e2a42fb`/`e9fd487`)** — `/` now renders `HomePreview`;
  `/home` still redirects to `/`; `/home-preview` renders the same component
  as the lab. No stale imports of `pages/Home.js` remain anywhere in
  `frontend/src`, no links to `/home` exist to go stale, and the theme-scope
  guard (`isLab = useLocation().pathname.startsWith('/home-preview')`)
  correctly gates `theme-preview` off `/` and applies `home-v2` on both
  routes, matching CLAUDE.md's updated rule. `noindex` is conditioned on
  `isLab` too, so `/` stays indexable and `/home-preview` doesn't.
- **Pricing-rule unification** (`7734488`, `ba23b17`, `cb91903`, `1df9092`,
  `2e37dd1`, `3b8276a`) — read through the diffs for `PropertyList.jsx`,
  `BookingSidebar.jsx`, `PricingAuditBanner.jsx`, `ListingsTab.jsx`,
  `StaysCard.jsx`, `StaysMapView.jsx`, `ChatHeader.jsx`. All are clean
  refactors onto the shared `utils/listingPrice.js` (`shownPrice`,
  `regularShown`, `holidayShown`, `unitLabel`, `priceSymbol`) — no control,
  handler or navigation target changed; every new backend field the banner
  reads (`totals.high_nightly`, `thresholds.high_nightly_ils`) is actually
  returned by `admin_import/properties.py`. No broken controls found.
- **Admin demand/visitor tracking** (`702a0de`, `45559c7`, `5db7bd0`,
  `d78bc9d`) — `GET /admin/metrics` and `GET /admin/metrics/by-user` are both
  called (`OverviewTab.jsx`, `DemandByPerson.jsx`); every field the backend
  returns (`site_visitors`, `site_pageviews`, `service_views`,
  `whatsapp_clicks`, `qr_scans`, `views_since`, `site_since`) has a matching
  render in `OverviewTab.jsx`. `range` param sent correctly; `by-user`'s
  search box is client-side filtering (not a missing server param — `limit`
  has a sane default and isn't needed). PostHog removed cleanly, replaced by
  `SiteVisitTracker.jsx`, wired in `App.js`.
- **Account deletion cascade** (`069d78b`) — `DELETE /admin/users/{id}` is
  called from `UsersTab.jsx`'s delete control; backend test
  (`test_admin_delete_user_cascade.py`) seeds one row per cascaded
  collection.
- **Storage-type removal** (`2c6240e`, gated further by `7dc5b044`) —
  `storage` is absent from `frontend/src/constants/propertyEnums.js`'s
  `RENTAL_TYPES` (confirmed, with an explicit comment explaining why), so it
  can't be selected in the create form or CSV upload. Every remaining
  frontend reference to `'storage'` (`ManagerPage.js`, `Properties.js`,
  `AddPropertyModal.jsx`, `ListingsFilterBar.jsx`) is a read/label/filter
  path for *existing* storage listings, exactly matching the commit's stated
  scope ("Everything that READS the value is untouched").
- **Business subdomains** (`a3bb375`, `93c88ce`) — correctly flag-gated:
  `SUBDOMAINS_ON = process.env.REACT_APP_BUSINESS_SUBDOMAINS === '1'`,
  defaults off, `WebAddressField.jsx` shows the path-based address unless the
  flag is set. Matches `docs/business-subdomains-setup.md`'s "stays off
  until the certificate exists." No premature UI promise found.
- **Courier delivery panel 409 handling** (`7dc5b044`) — confirmed in
  `DeliveriesTab.jsx`: a 409 on a stop update now calls `setMode(null)` +
  `onChanged()` to reload, per the commit message.
- **Contract sign compare-and-swap** (`7dc5b044`) — `routes/contracts.py`
  carries the "not yet signed" filter on the write per the commit; not
  independently re-verified against a live DB this run (static read only).
- **Smart Lists access tests / single business-page CTA** (`7f999fb`) —
  `BusinessPage.jsx` has exactly one supply CTA (`to="/join"`), confirmed via
  grep; no leftover `/signup` link.
- **Restore drill** (`89e553c`) — infra-only (a Windows scheduled task
  wrapper script), no admin UI references it and none should exist; not a
  dead-end candidate.
- **Orphaned-component sweep** — ran the skill's Part 4 script across
  `frontend/src/components/**/*.jsx`. Two hits: `BlocksHero.jsx` (carryover,
  above) and `gradient-text.jsx` (new, above). No other orphaned component
  found; `AntiMetalButton`, `LiquidButton`, `FlowButton`,
  `CoverflowCarousel` (the hero's other new UI primitives) are all still
  imported by `HomePreview.jsx` and/or `Navigation.js`.
- **Route table** — every route in `App.js` resolves to a real imported
  component; `/terms` is the only net-new route since 09-14 and its only two
  known callers both resolve. No `to=`/`navigate()` target found pointing at
  a path missing from the table during this pass.

## Not checked

- **No live browser/dev server available this run** — nothing was verified
  by actually clicking through the app or via `scripts/screenshot.mjs` /
  `scripts/check-*.mjs`. All findings above are static (grep + diff +
  guard-comparison) analysis, same method as 09-14. Priority items to
  click-verify next time a browser is available: the `/terms` page renders
  correctly in both languages and at 375/768/1280 (it's brand-new and only
  statically reviewed here); the pricing-rule refactor's actual rendered
  output on `/stays`, a listing page and the map strip for a real holiday-
  only and a real short-term-nightly listing; the new admin Demand-by-Person
  table and Site-visitors cards against real data; the courier 409-reload
  fix, by racing two updates.
- **`backend/routes/contracts.py`'s compare-and-swap** — read the diff only;
  did not run `test_sign_token_expiry.py` / `test_store_orders_status_race.py`
  in this session to confirm they pass against current `origin/main`.
- **`scripts/test-theme-scope.mjs`** — not run; theme scoping on
  `HomePreview.jsx` was verified by reading the `isLab` gate logic, not by
  executing the check script.
- **Neighbourhood-name Hebrew coverage** (`2c6240e`'s note that ~300
  neighbourhood names are "a separate job") — out of scope for this audit
  (data-completeness, not a dead end); flagging only because the commit
  message explicitly deferred it.
